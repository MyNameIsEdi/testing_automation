[pytest]
markers =
    sanity: Tests for the critical path and basic functionality
    regression: Comprehensive test suite for all features
    smoke: Quick tests to verify the system is up and running
    errors_handling: Tests validating negative scenarios and error messages

addopts = -v --html=report.html --self-contained-html